import { Inter } from 'next/font/google';
import { Exo } from 'next/font/google';

export const exile = Exo({
    subsets: ['latin'],
    variable: '--font-exile',
    display: 'swap',
});
export const inter = Inter({
    subsets: ['latin'],
    variable: '--font-inter',
    display: 'swap',
});